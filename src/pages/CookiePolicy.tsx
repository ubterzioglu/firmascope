import StaticContentPage from "@/components/StaticContentPage";
import { cookiePolicy } from "@/lib/footer-pages";

const CookiePolicy = () => <StaticContentPage content={cookiePolicy} />;

export default CookiePolicy;
