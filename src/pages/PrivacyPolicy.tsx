import StaticContentPage from "@/components/StaticContentPage";
import { privacyPolicy } from "@/lib/footer-pages";

const PrivacyPolicy = () => <StaticContentPage content={privacyPolicy} />;

export default PrivacyPolicy;
